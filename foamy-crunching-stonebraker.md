# 手机端测试工具 v3.4 全量升级方案

## 产品五步愿景

```
① 拍照分析 → ② 给方案 → ③ 引导拍摄 → ④ 后期/AI修图 → ⑤ 发布分享
   └────── 当前阶段（手机Web测试）──────┘   └─ 原生App ─┘   └─ 平台 ─┘
```

当前聚焦前三步。本文档整合了：原有 v3.4 bug 修复 + 统一输出架构（摄影骨架+社媒包装）+ 速度体验优化 + Canvas 可视化引导。**每项标注工作类型**，方便按前端/后端/产品分批推进。

---

## 一、工作类型图例

| 标记 | 含义 |
|:--:|------|
| 🧠 | Prompt 工程（AI 输出结构/话语体系） |
| 🔧 | 后端（server.py） |
| 🎨 | 前端（index.html） |
| 📋 | 产品设计（知识库/话语/策略，不改代码） |

---

## 二、🧠 Prompt 重构：统一输出架构

> 核心原则：摄影知识做骨架，社媒语言做包装。不分"双栈"，所有用户看到同一套界面。摄影深度藏在「💡 为什么好看？」可折叠入口。

### 2.1 输出结构升级

**类型**：🧠 Prompt + 🔧 后端 + 🎨 前端

每个方案从当前 `name + action + perspective` 升级为七段式：

| 字段 | 含义 | 示例 | 新增/改造 |
|------|------|------|:--:|
| `name` | 方案名 | "让阳光给你打光" | 保留 |
| `prep` | 要准备什么 | "什么都不用准备，站过去就行" | **新增** |
| `where` | 空间锚点 | "站到门口那个大盆栽旁边" | **新增** |
| `do` | 动作指令 | "让你男朋友蹲下来，手机举到胸口高度" | **改造** |
| `result` | 效果预览 | "拍出来会有自然的慵懒感，阳光从侧面打在脸上" | **新增** |
| `why` | 摄影深度 | "低机位改变透视，地面线条向远处延伸..." | **新增** |
| `annotations` | Canvas 标注 | `[{type:"position", x:0.35, y:0.72, ...}]` | **新增** |

**方案完整数量**：每个方向 1-9 套。变化丰富的场景 5-9 套，简单的 2-4 套。不凑数。

### 2.2 方向卡片话语重构

**类型**：🧠 Prompt + 🎨 前端

| 方向 | 当前 | 改为 | subtitle 新增 |
|------|------|------|------|
| 🟢 | 现在就拍 | **不会出错** | "拍出来一定好看" |
| 🔥 | 最出片 | **朋友圈会问在哪拍的** | "比普通游客照好看" |
| ✨ | 脑洞大开 | **还能这样拍？** | "你想不到的视角" |

前端展示：方向卡片仍逐屏递进（🟢先出），每次只展示一张。新增 `subtitle` 和 `style_promise`（效果承诺语）展示。

### 2.3 风格翻译层

**类型**：🧠 Prompt

保留内部风格匹配逻辑（摄影理论驱动），但在输出中：
- `style`：内部风格名（如"日系清新"），前端可选择展示
- **新增** `style_promise`：效果语言翻译（如"干净透亮，像日剧里的画面"）
- **新增** `subtitle`：方向级别的效果承诺（如"拍出来一定好看"）

原则：用户不需要知道什么叫"日系清新"，只需要知道"拍出来干净透亮，像日剧里的画面"。

### 2.4 场景锚点强制约束

**类型**：🧠 Prompt

在 CREATIVE_PROMPT_TEMPLATE 中新增硬约束：

> 每个方案 do/where 中的空间指令必须引用视觉分析 JSON 中 `[观察]` 到的具体场景元素。不得使用任何不依赖具体场景就能执行的通用建议。

❌ 模板式："低角度仰拍，让人物显得修长"（任何场景通用）
✅ 场景锚点："站到左边那棵树的树荫边缘，让阳光从树叶间漏下来正好打在肩膀上"

### 2.5 四维审美追问（大众版）

**类型**：🧠 Prompt

在 creative prompt Step 5 新增（不打分，只查逻辑矛盾）：

```
□ 可执行性：一个完全不懂摄影的人，能看懂这个方案并执行吗？
  → 如果有术语或抽象描述，翻译成动作指令
□ 场景特异性：这个方案是否依赖了这张照片里具体的视觉元素？
  → 如果去掉场景锚点方案仍然成立，说明是模板——重写
□ 效果诚实性：result 描述的效果在这个光线/空间条件下真的能实现吗？
  → 如果硬光逆光却说"柔和轮廓"，自相矛盾——修正
□ 社交吸引力：拍出来的效果符合"世俗意义上的好看"吗？
  → 用户发朋友圈，会有人问在哪拍的吗？
```

### 2.6 新 Prompt 完整输出结构

**类型**：🧠 Prompt

```json
{
  "presence": "在场感受——2-3句",
  "insight": "一句话洞察——最动人的视觉锚点",
  "directions": [
    {
      "id": "now",
      "emoji": "🟢",
      "label": "不会出错",
      "subtitle": "拍出来一定好看",
      "style": "日系清新",
      "style_promise": "干净透亮，像日剧里的画面",
      "reason": "推荐理由——朋友分享口吻",
      "how": "一句话操作概述",
      "source_note": "来源标注",
      "plans": [
        {
          "name": "让阳光给你打光",
          "prep": "什么都不用准备，站过去就行",
          "where": "站到门口那个大盆栽旁边",
          "do": "让你男朋友蹲下来，手机举到胸口高度，微微仰拍",
          "result": "拍出来会有一种自然的慵懒感，阳光从侧面打在脸上，轮廓特别柔和",
          "why": "低机位改变了透视关系，地面线条向远处延伸...",
          "annotations": [
            {"type": "position", "x": 0.35, "y": 0.72, "label": "站这", "color": "#4ade80"},
            {"type": "frame", "x": 0.1, "y": 0.05, "w": 0.8, "h": 0.6, "label": "取景范围"},
            {"type": "arrow", "from": {"x": 0.5, "y": 0.9}, "to": {"x": 0.3, "y": 0.4}, "label": "面向光"}
          ]
        }
      ]
    }
  ],
  "search_quality": {"overall": "🟢/🟡/🔴", "honest_note": "..."},
  "discovered_styles": [{"name": "...", "source_type": "...", ...}],
  "techniques_used": [{"name": "...", "source_type": "...", ...}]
}
```

🔥 和 ✨ 方向如果没有合适内容，设为 `null`。每个方向的 plans 数量按场景灵活决定。

### 2.7 保留的现有 Prompt 优点

**类型**：🧠 Prompt（不改的部分）

- ✅ Step 1 "场景沉浸"——"想象你站在这个场景里，不要分类，去感受"
- ✅ "朋友分享口吻"——"像拍过很多照片的人指着画面说话"
- ✅ 口吻约束——禁止摄影术语直接出现、用"你"视角、允许意外
- ✅ 不凑数原则——"有话则长无话则短"

---

## 三、🔧🔧 后端改造（server.py）

### 3.1 输出格式修复（防崩溃）

**类型**：🔧 后端 | **优先级**：🔴 止血

- [ ] 新增 `normalize_creative_output()` 函数：检测 `directions` 是 dict (v2) 还是 list (v3)，自动转换
- [ ] Prompt 中新增反面示例：`❌ WRONG: "directions": {"now": {...}} ← v2 OBJECT` / `✅ CORRECT: "directions": [{...}] ← v3 ARRAY`
- [ ] JSON 解析失败时重试一次，提示"只输出 JSON"
- [ ] 新增 20MB 图片大小限制
- [ ] API key 启动检查
- [ ] 超时时间 240s

### 3.2 SSE 流式接口

**类型**：🔧 后端 | **优先级**：🔴 速度体验

- [ ] `/analyze` 改为 SSE（Server-Sent Events）流式接口
- [ ] 事件流：
  ```
  event: exif        → EXIF 提取完成
  event: vision      → 视觉分析完成，推送 presence + insight（前端立即展示 Step 1）
  event: directions  → 创意推理进行中，逐个推送方向卡片
  event: complete    → 全部完成，推送完整结果
  event: error       → 出错时推送错误信息
  ```
- [ ] 使用 `text/event-stream` 响应头
- [ ] 前端用 `EventSource` 或 `fetch` + `ReadableStream` 接收

### 3.3 EXIF + 视觉 API 并行

**类型**：🔧 后端 | **优先级**：🟡 速度体验

- [ ] `extract_exif()` 和 `call_doubao(vision)` 用 `threading` 并行执行
- [ ] 预期节省 ~2s

### 3.4 设备感知

**类型**：🔧 后端 + 🎨 前端 | **优先级**：🟡

- [ ] 新增 `DEVICE_CONTEXTS` 字典（5 套设备档案精简版）
- [ ] `/analyze` 接受 `device` 参数
- [ ] 按设备注入上下文到 creative prompt：可用焦段 + 优势 + 硬约束

### 3.5 EXIF 交叉验证

**类型**：🔧 后端 + 🧠 Prompt | **优先级**：🟡

- [ ] Prompt 中新增 EXIF 交叉验证检查点
- [ ] ISO≥800 但视觉"明亮" → 修正光线分析
- [ ] 快门<1/60s → 方案标注稳定支撑建议
- [ ] 无 EXIF → 完全依赖视觉分析

### 3.6 错误处理增强

**类型**：🔧 后端 | **优先级**：🔴

- [ ] 20MB 图片大小限制
- [ ] 并发请求锁（一次只处理一个请求）
- [ ] API key 启动检查（启动时验证，不等到请求时）
- [ ] 超时时间 240s

---

## 四、🎨🎨 前端改造（index.html）

### 4.1 渐进式 UI（三阶段）

**类型**：🎨 前端 | **优先级**：🔴 速度体验

当前：所有结果一次性返回后才展示 → 用户干等 2-3 分钟
改造后：

- [ ] **Phase 1**：SSE `vision` 事件到达 → 立即展示 Step 1（场景洞察）。用户 30s 内看到第一屏内容
- [ ] **Phase 2**：`directions` 事件逐个到达 → 逐屏展示方向卡片（🟢→🔥→✨），用户边读边等
- [ ] **Phase 3**：`complete` 事件到达 → 展示完整方案卡片
- [ ] 加载文案按实际 API 阶段变化：
  - "正在看你照片里的光线和色彩..."
  - "正在匹配最适合你的风格..."
  - "正在生成你的专属拍摄方案..."

### 4.2 逐屏递进式方向卡片

**类型**：🎨 前端 | **优先级**：🔴

替换"一次性展示全部三张卡片"为逐屏递进：
- [ ] 同一时间只展示一张方向卡片
- [ ] 底部两个按钮：「📸 就选这个 → 出方案」「下一个 →」
- [ ] 方向进度指示 "1/3 · 2/3 · 3/3"
- [ ] "下一个"依次展示 🟢 → 🔥 → ✨
- [ ] 显示新字段：`subtitle`、`style_promise`

### 4.3 方案卡片七段式展示

**类型**：🎨 前端 | **优先级**：🔴

替换 flat action text 为结构化展示：
- [ ] 📋 要准备什么（`prep`）
- [ ] 📍 你站哪（`where`）
- [ ] 🎬 这样做（`do`）
- [ ] ✨ 拍出来（`result`）
- [ ] 🖼️ 带 Canvas 标注的原图（`annotations` 渲染）
- [ ] 💡 为什么这样好看？（`why` —— 可折叠，默认收起）
- [ ] 🎯 换个思路（`perspective` —— 可选，有真正差异才展示）

### 4.4 Canvas 标注渲染引擎

**类型**：🎨 前端 | **优先级**：🔴 可视化引导

- [ ] 新建 `renderAnnotations(canvas, annotations, photoUrl)` 函数
- [ ] 支持的标注类型：
  - `position`：圆点标记 + 文字标签（"站这"）
  - `frame`：虚线矩形（"取景范围"）
  - `arrow`：弧形箭头（光/面向方向）
  - `height`：垂直标尺（相机高度）
  - `crop`：半透明遮罩 + 建议裁剪区域（"裁掉上方"）
  - `zoom`：同心框（焦段范围示意）
- [ ] 坐标系统：0-1 相对值，适配任何屏幕
- [ ] 颜色方案：统一 emoji 色系（🟢 `#4ade80` / 🔥 `#f59e0b` / ✨ `#a78bfa`）
- [ ] 标注支持点击查看详情 tooltip

### 4.5 设备选择器

**类型**：🎨 前端 | **优先级**：🟡

- [ ] 页面顶部新增 `📱 你的设备` 下拉菜单
- [ ] 选项：iPhone 17 Pro / iPhone 17 / iPhone 13-16 Pro / iPhone 13-16 / 安卓旗舰 / 其他相机
- [ ] 选择后随 FormData 发送 `device` 参数

### 4.6 新增数据展示区（可折叠）

**类型**：🎨 前端 | **优先级**：🟡

- [ ] 折叠式 section：🔍 搜索质量（`search_quality`）
- [ ] 折叠式 section：🎨 发现的风格（`discovered_styles`）
- [ ] 折叠式 section：🔧 使用的技法（`techniques_used`）
- [ ] 默认折叠，不影响主要阅读流

### 4.7 新增 CSS

**类型**：🎨 前端 | **优先级**：🔴

- [ ] Canvas 标注容器样式
- [ ] 方向卡片单张展示 + 进度指示器
- [ ] 方案七段式各段样式（prep/where/do/result/why 折叠）
- [ ] 设备选择器样式
- [ ] 折叠式 section 样式
- [ ] SSE 进度条样式
- [ ] `style_promise` / `subtitle` 展示样式

---

## 五、📋📋 产品基础建设

### 5.1 社交审美知识域

**类型**：📋 产品 | **优先级**：🔴

新建 `.claude/skills/zhichu/knowledge/social-media-patterns/`，包含 7 个文件：

- [ ] `xiaohongshu-hot.md`：小红书爆款拍照规律（什么类型照片最容易获得互动）
- [ ] `couple-posing.md`：情侣拍照——男朋友视角万能公式（显高/显瘦/氛围感）
- [ ] `group-photo.md`：闺蜜/多人合影——站位公式、前后层次、互动姿态
- [ ] `solo-travel.md`：单人旅行——"不是自拍"的打卡照拍法
- [ ] `food-cafe.md`：咖啡厅/美食——桌面构图、光线利用、手持姿势
- [ ] `slimming-angles.md`：显瘦角度公式——不同身材的机位/焦段/角度策略
- [ ] `atmosphere-hacks.md`：氛围感营造——前景虚化/玻璃反光/慢门/影子等低成本技法

每个文件格式：`## 规律名 → 适用场景 → 视觉公式 → 手机操作步骤 → 为什么好看 → 来源标注`

### 5.2 "为什么好看"内容策略

**类型**：📋 产品 | **优先级**：🟡

- [ ] 定义"为什么好看"的内容标准——不堆术语、说人话、让用户"哦原来是这样"
- [ ] 示例库：收集 20 个"摄影原理 → 人话翻译"的范例，作为 Prompt few-shot 参考
- [ ] "为什么好看"的前端交互：默认折叠，点击展开，再点击收起

### 5.3 三方向卡片定位文档

**类型**：📋 产品 | **优先级**：🟡

- [ ] 明确三个方向的设计意图差异（不只是换 emoji 和 label）
- [ ] 🟢 不会出错：安全感。不需要任何技巧，站这就行。焦虑型用户的 default choice
- [ ] 🔥 朋友圈会问：炫耀感。发出去会被赞，比普通游客照好看。社交驱动型用户
- [ ] ✨ 还能这样拍：惊喜感。用户没想到的视角。好奇心驱动型用户

---

## 六、📋 未来产品规划（不在本次实施范围）

### 6.1 Web → 原生相机过渡路径

- Web 阶段验证标注系统哪些元素用户觉得有用 → 原生相机叠加层只做验证过的
- Canvas 渲染逻辑（坐标系统、标注类型）可 1:1 映射到原生 Metal/OpenGL/CameraX
- 设备感知从下拉选择 → 原生 App 自动识别设备型号

### 6.2 引导拍摄（步骤③）

- 如果用分析的手机拍：调出原生相机 + 引导叠加层（类似华为"环重合"逻辑 + 我们的场景特异性标注）
- 如果用其他设备：根据输入的设备型号，提供适配该设备的焦段/参数建议

### 6.3 步骤④⑤（远期）

- ④ 后期处理：AI 修图（可选）
- ⑤ 发布分享：自建平台或跳转社交媒体

---

## 七、实施路线图

### 第一轮：🔴 止血 + 核心体验（建议先做）

| # | 项目 | 类型 | 预估工作量 |
|:--:|------|:--:|:--:|
| 1 | Prompt 完整重写（2.1-2.6 全部） | 🧠 | 2-3h |
| 2 | 输出格式修复 + 错误处理（3.1 + 3.6） | 🔧 | 0.5h |
| 3 | SSE 流式接口（3.2） | 🔧 | 1.5h |
| 4 | 渐进式 UI + 加载文案（4.1） | 🎨 | 1h |
| 5 | 方案七段式展示（4.3） | 🎨 | 1h |
| 6 | Canvas 标注渲染引擎（4.4） | 🎨 | 1.5h |
| 7 | 逐屏递进方向卡片（4.2） | 🎨 | 0.5h |
| 8 | 全部新 CSS（4.7） | 🎨 | 1h |

### 第二轮：🟡 增强体验

| # | 项目 | 类型 | 预估工作量 |
|:--:|------|:--:|:--:|
| 9 | EXIF + 视觉 API 并行（3.3） | 🔧 | 0.5h |
| 10 | 设备选择器 + 设备感知（3.4 + 4.5） | 🔧 + 🎨 | 1h |
| 11 | EXIF 交叉验证（3.5） | 🔧 + 🧠 | 0.5h |
| 12 | 数据展示折叠区（4.6） | 🎨 | 0.5h |
| 13 | 社交审美知识域 7 文件（5.1） | 📋 | 2-3h |
| 14 | "为什么好看"内容标准 + 示例库（5.2） | 📋 | 1h |

### 第三轮：🟢 验证 + 迭代

| # | 项目 | 类型 | 预估工作量 |
|:--:|------|:--:|:--:|
| 15 | 真手机实拍测试（8 项验证） | 🎨 + 🔧 | 1h |
| 16 | 标注类型用户测试（哪些看得懂/看不懂） | 📋 | 待定 |
| 17 | 三方向卡片定位文档（5.3） | 📋 | 0.5h |
| 18 | 找 10 个非摄影素人测试 | 📋 | 待定 |

---

## 八、测试策略

1. **格式修复验证**：上传照片，浏览器 console 确认 `directions` 始终是 array
2. **SSE 流式验证**：确认 Phase 1/2/3 按序展示，非一次性全部出现
3. **逐屏递进验证**：Step 2 同一时间只有 1 张卡片，可依次切换
4. **方案七段验证**：确认 prep/where/do/result/why/annotations 六段全部展示
5. **Canvas 标注验证**：确认各类型标注正确渲染在原图上，坐标不偏移
6. **Why 折叠验证**：确认"为什么好看"默认折叠、点击展开、再次点击收起
7. **设备感知验证**：同一张照片选不同设备，焦段建议应有差异
8. **审美验证存在验证**：检查 prompt 文本包含四维追问（大众版）
9. **EXIF 交叉验证**：用有 EXIF 的真手机照片测试
10. **场景锚点验证**：检查方案中的 where/do 是否引用了视觉分析中的具体元素

---

## 九、关键设计决策记录

1. **不分栈，统一输出**：摄影栈和社交栈不在前端区分。所有用户看到同一套界面。摄影深度藏在"为什么好看"可折叠入口
2. **摄影骨架 + 社媒包装**：三层判断——摄影层（硬约束）≥ 个体层（场景锚点）> 社交层（包装验证）
3. **速度问题用感知解决**：短期不做更快，做"不让人觉得在等"——SSE 流式 + 渐进 UI
4. **可视化问题用 Canvas 解决**：不做 AI 生图，在原图上绘制标注——零成本、即时、桥接未来原生相机
5. **Web 阶段 = 原生相机的试验场**：标注系统的坐标格式、元素类型、用户反馈，直接决定原生叠加层的设计
