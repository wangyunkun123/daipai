# 带拍 · 项目状态与操作手册

> 最后更新：2026-07-26 | 当前版本：daipai v3.5

---

## 一、当前架构：daipai v3.5

### 1.1 核心设计原则

```
AI 是引擎，不是兜底。规则是护栏，不是引擎。知识库是缓存，不是真理。
诚实比完整重要——没东西就不给，不编造。
风格先于技法——先找到方向，再在方向上丰富执行细节。
```

| 层 | 角色 | 说明 |
|----|------|------|
| 🧠 AI 引擎层 | 主导 | 搜索发现 → 场景沉浸 → 洞察驱动 → 方案生成（一次统一推理） |
| 🛡️ 规则护栏层 | 标注 | 光线/设备不排除——只诚实标注 🟢🟡🔴，由方向选择器路由 |
| 📚 知识缓存层 | 加速 | S/A/B/C 四级命中梯度搜索；新发现自动注册沉淀 |
| 🎯 诚实机制 | 全覆盖 | 搜索/方向/方案/技法/生图——每个环节搜不到就诚实说 |

### 1.2 精简后 AI 模块（5-6 个实际调用）

| # | 模块 | 调用方式 | 每次消耗 |
|:--:|------|------|:--:|
| 1 | 豆包视觉 API | 外部 API | 1 次 |
| 2 | WebSearch 风格搜索 | 外部搜索 | 2-5 次（优先级分层，条件触发） |
| 3 | WebSearch 技法搜索 | 外部搜索 | 0-3 次（风格确定后触发 / 风格无效时备用模式） |
| 4 | 统一 AI 引擎（合并 综合判断+审美主张） | LLM 推理 | 1 次（6 步） |
| 5 | 方向选择卡片 | LLM 生成 | 1 次（1-3 屏按需） |
| 6 | 方案生成 + 自检 | LLM 生成 | 1-2 次（弹性 1-9 套） |
| — | 生图提示词 | LLM 生成 | 默认不生成 |

### 1.3 工作流（5 阶段）

```
用户拍一张照片
    ↓
[阶段 0] 环境感知（静默，如有 GPS）
    ├─ 0A EXIF → 0B 地点 → 0C 太阳 → 0D 天气 → 0E 经验搜索 → 0F 时域调度
    ↓
[阶段 1] 场景分析 + AI 引擎（静默）
    ├─ 1A 豆包视觉 API 识图（🚨 硬约束——观察/推测标记 + mood_axes + uncertainty）
    ├─ 1B 题材检测 + 设备能力加载 + EXIF 交叉验证（创作边界前置）
    ├─ 1C 统一 AI 引擎 v4（一次推理，6 步）：
    │   ├─ Step 1: 搜索结果综合 → 发现风格方向 + 来源权重
    │   ├─ Step 2: 规则诚实标注（🟢🟡🔴🟠）+ 安全红线
    │   ├─ Step 3: 场景沉浸 + 洞察提炼（必须有视觉锚点）
    │   ├─ Step 4: 创作方向生长（10 种类型，按场景自然选择）
    │   ├─ Step 5: 设备翻译（优势思维，非降级思维）
    │   └─ Step 6: 审美筛除（只筛"不应该"，技术限制→方向选择器）
    │   ├─ 1C.2 技法搜索（风格确定后触发——主线/备用双模式）
    │   └─ 1C.3 EXIF 交叉验证（豆包 uncertainty 字段优先复查）
    └─ 1D 方向预选（三级协调：全量发现→审美筛除→方向分配）
    ↓
[阶段 2] 🎬 方向呈现 ← 诚实版
    ├─ 🟢 现在就拍：每个场景必有
    ├─ 🔥 最出片：有才展示
    └─ ✨ 脑洞大开：有才展示
    ↓
[阶段 3] 方案生成（用户选定方向后）
    ├─ 🥉🥈🥇场景评估 → 弹性数量 1-9 套
    ├─ 🥇丰富场景 → 启用组图节奏（全景→中景→近景→特写）
    ├─ 每套：要准备什么 → 就这么拍 → 💡 → 🎯（按需）
    └─ 内置四维追问自检（不打分）
    ↓
[阶段 3.5] 自动知识沉淀（静默）
    ├─ 检查匹配关系是否在缓存中 → 不在则自动注册
    ├─ 检查技法是否在技法缓存中 → 不在则自动注册
    └─ 在 → 验证计数 +1
    ↓
[阶段 4] 拍摄指导
    ├─ 设备翻译（方案→设备具体操作语言）
    ├─ 📸 拍摄计划总览表
    └─ 🔮 创作探索（可折叠）
```

---

## 二、v3.2 → v3.4 关键变更

### 2.1 架构精简

| 指标 | v3.2 | v3.4 | 节省 |
|------|:--:|:--:|:--:|
| 每次搜索调用 | 7-11 次 | 2-7 次 | ~40-50% |
| LLM 推理步骤 | 4-5 次独立调用 | 2-3 次 | ~40% |
| 生图提示词生成 | 每套方案必出 | 默认不出 | ~95% |
| 方向卡片 | 3 张全出 | 1-3 张按需 | 0-66% |
| 方案套数 | 3-4 套（硬凑） | 1-9 套（弹性） | 场景决定 |

### 2.2 诚实机制全覆盖

| 场景 | 诚实行为 |
|------|------|
| 搜索全部无用 | 告知"基于场景分析，非搜索结果" |
| 无高辨识度方向 | 🔥卡片不展示 |
| 无脑洞方向 | ✨卡片不展示 |
| 场景无法出片 | 告知换场景/时间 |
| 方案无价值 | 1 套最可行方案 + 诚实说明 |
| 技法无启发 | 🎯换个思路不写 |
| AI生图不开眼界 | 🤖默认不出现 |

### 2.3 风格→技法先后关系

```
v3.2：风格/技法并行搜索 → AI 自己判断怎么融合
v3.4：先找风格 → 风格确定后在风格下搜技法 → 风格无效时备用模式（技法出发）
```

### 2.4 审美验证去打分化

```
v3.2：1-10 分自评 — AI 给自己永远打 7-8 分，形同虚设
v3.4：四维追问自检 — 不通过的是逻辑矛盾（不是"分数不达标"）
```

---

## 三、已完成 vs 规划中

### 3.1 ✅ 已完成

| 层 | 内容 | 规模 | 状态 |
|----|------|------|:--:|
| 📷 理论层 | 构图/光线/色彩/焦段/景深/空间/景别/姿势/时机/场景/天气/视觉重心/曝光三角/后期意识 | 14 域 | ✅ |
| 🎨 审美层 | 审美语法/视觉心理学/视觉叙事/风格配方/情绪词库/跨媒介风格/质感审美/组图节奏/大师风格 | 10 域 54 文件 | ✅ |
| 🔧 设备层 | iPhone 17 Pro / 17 / 13-16 Pro / 13-16 标准版 / 安卓旗舰 + 翻译总表 | 5 档案 | ✅ |
| 🧠 匹配路由 | 13 个路由器 v3.5（光线/色彩/空间/质感/题材/情绪/风格/大师/姿势/审美主张/方向选择/创意张力/技法路由） | 13 文件 | ✅ |
| 🔧 外部脚本 | EXIF提取/太阳位置/地点识别/时域调度/豆包视觉API | 5 脚本 | ✅ |
| 📋 SKILL.md | v3.5 完整工作流 + 输出格式 | 主文件 | ✅ |
| 📚 知识压缩 | knowledge-core.md——所有决策参数的压缩速查表 | 1 文件 | ✅ |
| 🎨 风格配方 | 17 种风格（含便利店美学/港风复古/森系/法式慵懒/新中式/梦幻花语） | 17 文件 | ✅ |
| 📋 测试验证 | 10 张实拍照片完整 Pipeline 测试 | 2 文件 | ✅ |
| 📋 项目文档 | 安全审查/功能审查/产品架构/AI提示词全览/v4优化计划 | 5 文件 | ✅ |

### 3.2 ✅ 移动端测试工具 v3.5（🆕）

| 功能 | 说明 | 状态 |
|------|------|:--:|
| 设备栏直接下拉 | 移除"Apple"前缀 + "拍摄设备"标签 + EXIF自动识别标注 | ✅ |
| 轮播滑动导航 | 方向卡片/方案卡片 CSS scroll-snap 原生滑动 + 圆点指示器 | ✅ |
| 渐进式展示 | EXIF → 场景 → 方向卡片 → 方案按需生成（SSE流式） | ✅ |
| 图片预览适配 | `object-fit: contain` + `50vh`，竖/横/方形全兼容 | ✅ |
| Canvas 视觉标注 | 原图叠加站位/取景/裁剪标注，坐标自适应 | ✅ |
| 生图提示词增强 | 注入设备(相机+镜头) + 光线条件 + 色彩色调 + 9要素公式 | ✅ |
| 方案缓存 | 同方向避免重复调用豆包 API | ✅ |
| JSON 截断恢复 | 鲁棒解析 + max_tokens 8000 | ✅ |
| 小红书热门相机 | Sony A7M4/A7C2/A6400 + Fujifilm X-T5/X-T50/X100VI + Ricoh GR3 + Canon R6II | ✅ |
| 镜头档案系统 | 可换镜头相机支持镜头选择（24-70/85/35/50等9种镜头） | ✅ |
| 设备上下文注入 | 生成方案时注入设备能力档案（strengths/limits/capability） | ✅ |
| 风格缓存 | `style_cache.json` 自动积累，474条已验证匹配 | ✅ |
| 位置+天气 | GPS→地点反向编码 + 太阳位置 + 时域调度（黄金/蓝调时刻） | ✅ |

**手机访问**：`http://<电脑IP>:8888`（手机和电脑同WiFi）

### 3.3 🔲 待完成

| 优先级 | 内容 | 说明 |
|:--:|------|------|
| 🟡 | 天气 API 集成 | wttr.in 已设计，待实际联调 |
| 🟡 | 经验聚合自动化 | 0E 阶段已有框架，待完善搜索策略 |
| 🟢 | 用户偏好学习 | 长期——根据用户选择记录优化推荐 |
| 🔵 | 地图/街景集成 | 远期——建筑朝向+遮挡分析 |
| 🟡 | 搜索变量质量自检 | AI 填完搜索词后自检"搜索引擎会返回有用结果吗" |
| 🟢 | 场景拆解搜索 | 把场景拆成 3-4 个元素各搜一次，提高搜索精度 |
| 🟢 | 移动端七段方案升级 | prep/where/do/result/why/annotations 七段结构 |
| 🟢 | 真手机实拍测试 | 8项验证清单（格式/SSE/逐屏/Canvas/设备感知等） |

---

## 四、知识库文件地图

```
knowledge/
├── _compressed/
│   └── knowledge-core.md          ← 🧠 决策参数速查
├── matching/                       ← 🧠 匹配路由层 v3.5（13 文件）
│   ├── _index.md                   → 总纲 + 三级协调协议 v2.3
│   ├── style-router.md             → 风格路由 v2.4（优先级分层+权重+诚实）
│   ├── technique-router.md         → 技法路由 v1.1（主线/备用双模式+诚实）
│   ├── aesthetic-assertion.md      → 审美主张引擎 v2.4（合并统一推理）
│   ├── direction-selector.md       → 方向选择器 v2.4（诚实版——按需展示）
│   ├── light-router.md             → 光线分析
│   ├── color-router.md             → 色彩策略
│   ├── space-router.md             → 构图候选
│   ├── texture-router.md           → 质感策略
│   ├── genre-router.md             → 题材优先级
│   ├── mood-router.md              → 情绪→视觉参数（接收 mood_axes 三维输入）
│   ├── master-router.md            → 大师匹配
│   ├── posing-router.md            → 姿势引导
│   └── creative-tension.md         → 跨维度张力
├── style-recipes/                  ← 🎨 17 种风格配方
│   └── _index.md                   → 风格矩阵 + one_liner 全局标识
├── device-adaptation/              ← 🔧 5 设备档案 + 翻译总表
├── aesthetic-grammar/              ← 审美语法四柱 + 批判框架
├── mood-lexicon/                   ← 9 种情绪词库
├── cross-media-styles/             ← 20 种跨媒介风格翻译
├── masters/                        ← 32 位大师风格库
└── [... 14 理论域]                 ← 构图/光线/色彩/焦段/...
```

---

## 五、方案生成规范 v3.5

### 5.1 生成前

- [ ] 场景评估完成（🥉🥈🥇）
- [ ] 方案数量与场景等级匹配（1-3 / 3-6 / 6-9）
- [ ] 🥇场景启用组图节奏（全景→中景→近景→特写）
- [ ] 所有方案统一在用户选定的风格下
- [ ] 先保证摄影方案价值，再保证差异化

### 5.2 生成后（四维追问自检）

- [ ] 叙事完整性：方案有清晰的视觉叙事吗？不是"站这里按快门"
- [ ] 调性统一度：色调/影调/质感在风格框架内吗？没有跑偏
- [ ] 取舍合理性：排除了不该出现的东西吗？排除有理由吗
- [ ] 破格目的性：有破格吗？有的话是有意为之吗

### 5.3 诚实兜底

- [ ] 场景不适合拍 → 诚实告知换场景/时间 + 给 1 套最可行方案
- [ ] 🎯换个思路 → 有真正的观看方式变化才写
- [ ] 🤖AI生图 → 默认不出，只在真能打开思路时出

---

## 六、最近更新记录

### 2026-07-26（v3.5 移动端测试工具）

| 批次 | 变更 | 说明 |
|:--:|------|------|
| 1 | 设备栏重构 | 直接下拉选择 + "拍摄设备"标签 + EXIF自动识别标注 + 镜头选择 |
| 2 | 轮播滑动导航 | CSS scroll-snap 原生滑动 + 圆点指示器，替代按钮切换 |
| 3 | 生图提示词增强 | 注入设备(相机+镜头) + 光线条件 + 色彩色调 + 9要素豆包公式 |
| 4 | 图片预览适配 | `object-fit: contain` + `50vh`，竖版/横版/方形全兼容 |
| 5 | JSON鲁棒解析 | 截断恢复 + max_tokens 8000，2/3测试用例通过 |
| 6 | 方案缓存 + 按需生成 | 同方向缓存避免重复调用 + SSE流式渐进展示 |
| 7 | 小红书相机 + 镜头系统 | 9款热门相机 + 9种镜头档案 + 设备上下文注入方案生成 |
| 8 | 风格缓存 | style_cache.json 自动积累474条已验证匹配 |

### 2026-07-24（v3.4 优化批次）

| 批次 | 变更 | 文件 | 说明 |
|:--:|------|------|------|
| 1 | 豆包视觉 prompt 优化 | `doubao-vision.py` | 观察/推测标记 + mood_axes 三维输出 + uncertainty 字段 |
| 1 | 风格搜索重构 | `style-router.md` v2.4 | 优先级分层（三梯队）+ 来源权重（≥10万粉/≥5000赞）+ 诚实机制 |
| 1 | 技法搜索重构 | `technique-router.md` v1.1 | 并行→先后（主线/备用双模式）+ 诚实机制 + 展示不分家 |
| 2 | 合并综合判断+审美主张 | `SKILL.md` §1C | 从两次 LLM 推理合并为一次统一推理（6 步） |
| 2 | 方向卡片诚实 | `SKILL.md` §2, `direction-selector.md` v2.4 | 🔥✨改为按需展示、硬编比不编更差 |
| 2 | 方案弹性数量 | `SKILL.md` §3B | 🥉🥈🥇场景评估 + 1-9 套 + 组图触发 + 诚实兜底 |
| 3 | 生图提示词精简 | `SKILL.md` §生图 | 前期优先、🤖默认不出、删除关键词解读 |
| 3 | 审美验证去打分 | `SKILL.md` §4D | 1-10 分自评 → 四维追问自检 |

### 2026-07-23（v2→v3 架构演进）

| 变更 | 说明 |
|------|------|
| 豆包视觉 API 集成 | 8 字段结构化识图 → v3.4 升级为观察/推测标记 |
| EXIF 交叉验证 | 豆包主观感受 × EXIF 物理事实 → v3.4 优查 uncertainty 标记字段 |
| 三级协调协议 v2.3 | 全量发现→审美筛除→方向分配，消除"排除"歧义 |
| 审美主张引擎 v2.0 | 从分类标签升级为洞察驱动的创作方向生长 |
| 技法路由 v1.0 | AI 自由搜索前期拍摄技巧 → v3.4 改为风格后触发 |

---

> 版本：v3.5 | 日期：2026-07-26 | 变更：移动端测试工具全面升级 + SKILL.md v3.5 效率优化 + 品牌生态更新
