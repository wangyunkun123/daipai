# 城市街拍 · 场景知识注入 v2

> 状态：✅ 本地部署完成，待测试验证

---
## 架构变更（v1 → v2）

v1 方向错了——街拍创建了平行 Prompt 路径（STREET_DIRECTIONS_PROMPT 替换 DIRECTIONS_PROMPT）。
v2 改为**知识注入**：街拍知识融入统一流程，不创建平行路径。

```
Before（v1 错误）:                  After（v2 正确）:
scene_mode == 'street'           scene_mode == 'street'
    ↓                                ↓
STREET_DIRECTIONS_PROMPT          DIRECTIONS_PROMPT（统一，不变）
（替换整个 Prompt）                  + knowledge_context 含街拍知识
    ↓                                ↓
STREET_PLANS_CONTEXT              PLANS_PROMPT（统一，不变）
（前置独立段落）                      + scene_execution_context（正向执行原则）
                                    + forbidden_constraints（街拍禁止项）
```

## 核心改动

1. **删除** `STREET_DIRECTIONS_PROMPT`（~117行）和 `STREET_PLANS_CONTEXT`（~34行）
2. **保留** `STREET_KNOWLEDGE`——作为知识资产
3. **新增** `SCENE_KNOWLEDGE_REGISTRY` 字典——每个场景包含 keywords / knowledge_text / execution_principles / forbidden_constraints
4. **新增** `detect_scene_mode()` 和 `build_scene_execution_context()` 辅助函数
5. **统一 Prompt**：DIRECTIONS_PROMPT 和 PLANS_PROMPT 不再分支，场景知识通过占位符注入
6. 方向标签保持通用：🟢现在就拍 / 🔥最出片 / ✨脑洞大开

## 未来扩展

添加咖啡厅/日落等场景只需向 SCENE_KNOWLEDGE_REGISTRY 添加条目——**只加数据，不加流程**。

---
## 如何测试

```
手机访问 http://192.168.3.72:8888
1. 拍一张街景照片（不需任何手动选择）
2. 看三个方向是否仍是 🟢/🔥/✨（通用标签）
3. 看方向内容、方案是否融入了街拍大师视角和执行原则
4. 确认上传非街景照片时通用模式不受影响
```
