# 带拍 · 网站建设手册

> 最后更新：2026-08-31 | 状态：v4.0 运行中 + ICP 备案已完成（香港测试机已停用）

---

## 一、服务器总览

| | 🔵 大陆服务器（正式） |
|------|------|
| 云产品 | 阿里云 ECS 经济型 e 实例 |
| 实例 ID | `i-2zediimbfwg5mk3oxnjj` |
| 公网 IP | `123.56.229.172` |
| 地域 | 华北2（北京） |
| 配置 | 2C2G / 40G / 3M独享 |
| 系统 | Ubuntu 22.04 |
| 费用 | ¥99/年 |
| 用途 | 正式运行（guidepic.com / guidepic.cn 同服） |
| 备案 | ✅ 已完成 |

> 香港轻量云（`d4f6f5e1bf2f49e681644a929ff4b49e`，47.82.117.17）已于 2026-08 停用下线。

### 登录

```bash
ssh root@123.56.229.172
```

> ⚠️ 密码相同，勿泄露。建议以后改成密钥登录。

---

## 二、域名

| 域名 | 状态 | DNS 指向 | 用途 |
|------|:--:|------|------|
| `guidepic.cn` | ✅ 运行中 | `123.56.229.172`（北京） | 备用域名，与 com 同服 |
| `guidepic.com` | ✅ 已备案运行 | `123.56.229.172`（北京） | 正式入口 |
| `daipai.app` | ❌ 待注册 | — | 备用 |

### 当前 DNS 设置（阿里云万网）

| 域名 | 记录 | 值 | 状态 |
|------|------|------|:--:|
| guidepic.cn | @ / www → `123.56.229.172` | 北京 IP | ▶ 启用 |
| guidepic.com | @ / www → `123.56.229.172` | 北京 IP | ▶ 启用 |

> 香港轻量云服务器（47.82.117.17）已于 2026-08 停用下线，全部流量收敛到大陆服务器。

---

## 三、软件架构

### 大陆服务器（正式运行）

```
用户手机 → https://guidepic.com
                │
                ▼
           Nginx (80/443, SSL)
                │
                ▼
           Flask (127.0.0.1:8888)
                │
   ┌────────────┼────────────┐
   ▼            ▼            ▼
exiftool    豆包视觉API    SSE流式输出
(EXIF)    (识图分析)     (方案推送)
```

### 安装的软件

| 软件 | 大陆 | 用途 |
|------|:--:|------|
| Python 3.12 | ✅ | Flask 应用 |
| Nginx | ✅ | 反向代理 |
| Git | ✅ | 拉取代码 |
| exiftool | ✅ | 照片 EXIF 提取 |
| certbot | ✅ | SSL 自动签发续期 |
| Flask + Pillow | ✅ | Web 框架 + 图片处理 |

---

## 四、服务器管理

### 目录结构

```
/opt/daipai/                       ← 项目根目录
/opt/daipai/mobile-tester/         ← 移动端测试工具
/opt/daipai/mobile-tester/server.py ← 主程序
/opt/daipai/mobile-tester/.env     ← 豆包 API 密钥（600权限）
/opt/daipai/mobile-tester/venv/    ← Python 虚拟环境
```

### systemd 服务

```
服务名：daipai
配置：/etc/systemd/system/daipai.service
自启：已启用
```

### 常用命令

```bash
# 服务状态
systemctl status daipai

# 重启（代码更新后）
systemctl restart daipai

# 实时日志
journalctl -u daipai -f

# Nginx 重载
systemctl reload nginx

# 测试 Nginx 语法
nginx -t
```

### 更新代码流程（v4.0 自动部署）

**方式一：GitHub Webhook（推荐）**
```bash
# 本地 push → GitHub → 服务器自动拉取 + 重启
git push  # 就这一步！30 秒内 guidepic.com 自动更新
```
详见 `docs/自动部署配置.md`

**方式二：手动部署（备用）**
```bash
ssh root@123.56.229.172 "cd /opt/daipai && bash deploy.sh"
```

### 防火墙（阿里云控制台 → 轻量服务器 → 防火墙）

| 端口 | 用途 |
|------|------|
| 22 | SSH |
| 80 | HTTP |
| 443 | HTTPS ✅ |
| 8888 | Flask 直连（调试） |

### Nginx 配置

```
文件：/etc/nginx/sites-available/daipai
关键：proxy_buffering off    （SSE 必须）
      proxy_read_timeout 300s（AI 超时保护）
      client_max_body_size 25M
```

### SSL 证书

```
签发工具：Let's Encrypt (certbot)
到期时间：2026-10-24
自动续期：已配置（每 90 天）
```

---

## 五、关键配置

### 图片压缩（2026-07-26 修复）

```
问题：大图上传 4 分钟超时
修复：server.py 第 38-39 行
  MAX_IMAGE_DIM = 2048   最长边 > 2048px 等比缩放
  JPEG quality = 85      质量降到 85%

效果：5712×4284(4.7MB) → 2048×1536(~300KB)
      上传快 10 倍+，对 AI 识图无影响
```

### 豆包 API

```
密钥：/opt/daipai/mobile-tester/.env（权限 600）
格式：DOUBAO_API_KEY=ark-xxx
```

---

## 六、ICP 备案追踪

| 阶段 | 状态 | 日期 |
|------|:--:|------|
| 提交初审 | ✅ 已完成 | 2026-07-26 |
| 阿里云初审 | ✅ 通过 | — |
| 工信部短信核验 | ✅ 通过 | — |
| 管局终审 | ✅ 通过 | — |
| 备案通过 | ✅ 完成 | 2026-08 |

### 备案详情

```
备案类型：个人
域名：guidepic.com
服务器：i-2zediimbfwg5mk3oxnjj（北京 ECS）
网站名：带拍 AI拍照灵感指南
```

### 备案完成后的迁移（已执行）

```
1. ✅ guidepic.com DNS 恢复解析
2. ✅ 大陆 ECS 部署完整环境
3. ✅ guidepic.com 正式上线
4. ✅ 退掉香港服务器（¥39/月）
5. guidepic.cn 保留为跳转/备用域名
```

---

## 七、待办

- [x] 香港服务器上线（已停用）
- [x] guidepic.cn SSL 配置
- [x] 图片上传超时修复
- [x] 北京 ECS 到位
- [x] guidepic.com 域名注册
- [x] ICP 备案通过
- [x] 备案后正式部署大陆服务器
- [x] 退订香港轻量服务器
- [ ] 注册 daipai.app
- [ ] 定期备份方案

---

## 八、故障排查

| 问题 | 先查什么 | 命令 |
|------|------|------|
| 网站打不开 | 服务状态 | `systemctl status daipai` |
| 图片分析失败 | 错误日志 | `journalctl -u daipai -n 50` |
| 豆包 API 异常 | 密钥有效？ | `cat /opt/daipai/mobile-tester/.env` |
| 端口不通 | 防火墙 | 阿里云控制台查规则 |
| Nginx 502 | Flask 挂了？ | `systemctl status daipai` |

### 快速重启

```bash
ssh root@123.56.229.172
systemctl restart daipai && systemctl reload nginx
# 不行就：
reboot
```
