# 带拍 · 云端部署与分发计划

> 日期：2026-07-26
> 目标：将移动端测试工具部署到云端，让朋友/测试用户通过手机浏览器访问使用
> 当前状态：本地 Python Flask 开发服务器（仅同WiFi可用）

> 📌 **2026-08 更新：香港轻量云阶段已执行完毕并停用。** 当前线上为大陆服务器（guidepic.com → 123.56.229.172，ICP 备案已完成）。下文阶段一为历史记录，仅作参考，勿再执行。

---

## 一、先搞清楚：ICP 备案是什么

### 一句话解释

在中国大陆，**任何能让公网访问的网站都必须备案**，否则会被防火墙拦截。这是法律要求，没有捷径。

### 备案需要什么

| 条件 | 说明 |
|------|------|
| 一台中国大陆的云服务器 | 阿里云/腾讯云/华为云均可 |
| 一个已注册的域名 | guidepic.com ✅ 已注册 |
| 实名认证 | 身份证 + 人脸识别 |
| 服务器提供商审核 | 在你的云服务器商提交备案 |
| 等待时间 | **约 20 个工作日（1个月）** |
| 备案完成后 | 域名才能正常访问你的网站 |

### ⚡ 但是！测试阶段可以绕过

**香港服务器不需要 ICP 备案。** 这是测试阶段的最佳方案：

| | 香港服务器 | 大陆服务器 |
|------|:--:|:--:|
| ICP 备案 | ❌ 不需要 | ✅ 必须 |
| 立即可用 | ✅ 买完就能用 | ❌ 等1个月 |
| 国内访问速度 | 🟢 快（30-50ms） | 🟢 很快（10-20ms） |
| 合规 | 🟢 完全合法 | 🟢 完全合法 |
| 成本 | ~¥50-100/月 | ~¥50-100/月 |
| 适用场景 | **测试/MVP 阶段** ← 你现在 | 正式上线后 |

---

## 二、推荐方案：三阶段部署路线

### ⚪ 阶段一：立即测试 —— 香港轻量云（历史，已停用）

```
用户手机 → guidepic.com → 香港服务器 → Flask App → 豆包AI
                              ↑
                         无需备案，买完即用
```

**推荐配置：**

| 项目 | 推荐 |
|------|------|
| 云厂商 | 阿里云 香港轻量应用服务器 / 腾讯云 香港轻量 |
| 配置 | 2核 CPU / 2GB 内存 / 40GB SSD |
| 带宽 | 3-5 Mbps（够 10-50 人同时用） |
| 系统 | Ubuntu 22.04 LTS |
| 月费 | ~¥50-70/月（轻量应用服务器） |
| 域名 | guidepic.com（已注册，DNS 解析到服务器 IP） |

### 🟡 阶段二：产品验证后（1-2个月后）—— 大陆服务器 + ICP

当产品验证通过、准备正式推广时：
- 购买大陆服务器（阿里云 ECS / 腾讯云 CVM）
- 提交 ICP 备案（约 20 个工作日）
- 备案完成后切换 DNS

### 🔵 阶段三：规模化（远期）

- 负载均衡 + 多实例
- CDN 加速静态资源
- 数据库（用户系统、拍摄记录）
- 监控告警

---

## 三、阶段一详细操作步骤

### Step 1：注册 guidepic.com 域名

> ⚠️ **现在立刻做！**

1. 打开 [阿里云万网](https://wanwang.aliyun.com) 或 [腾讯云域名](https://buy.cloud.tencent.com/domain)
2. 搜索 `guidepic.com`
3. 加入购物车，结算（约 ¥60-80/年）
4. 完成实名认证（需要身份证）

### Step 2：购买香港服务器

> ⚠️ **域名注册后立刻做**

#### 阿里云路径（推荐新手）

1. 打开 [阿里云香港轻量应用服务器](https://swas.console.aliyun.com)
2. 选择「中国香港」地域
3. 镜像选择：**Ubuntu 22.04**
4. 套餐选择：**2核 2GB 内存**（¥50-70/月）
5. 购买时长：先买 1 个月测试
6. 设置 root 密码

#### 腾讯云路径

1. 打开 [腾讯云香港轻量](https://buy.cloud.tencent.com/lighthouse?region=8)
2. 选择「中国香港」地域
3. 镜像选择：**Ubuntu 22.04**
4. 套餐：2核2G

### Step 3：DNS 解析（把域名指向服务器）

1. 在域名控制台，进入 DNS 解析
2. 添加两条记录：

| 记录类型 | 主机记录 | 记录值 | TTL |
|:--:|------|------|:--:|
| A | @ | 服务器公网IP | 600 |
| A | www | 服务器公网IP | 600 |

### Step 4：服务器环境搭建

SSH 登录服务器后，复制以下命令逐条执行：

```bash
# 1. 更新系统
sudo apt update && sudo apt upgrade -y

# 2. 安装 Python 和依赖
sudo apt install -y python3 python3-pip python3-venv nginx git

# 3. 安装图像处理库（PIL/Pillow 需要）
sudo apt install -y libjpeg-dev zlib1g-dev libfreetype6-dev liblcms2-dev

# 4. 创建应用目录
mkdir -p /home/guidepic/app
cd /home/guidepic/app

# 5. 克隆项目（或者用 scp 上传）
git clone <你的GitHub仓库地址> .

# 6. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 7. 安装 Python 依赖
cd mobile-tester
pip install flask gunicorn pillow python-dotenv

# 8. 安装 EXIF 提取依赖
pip install Pillow piexif  # 如果 exif-extract.py 需要

# 9. 配置环境变量
cat > .env << 'EOF'
DOUBAO_API_KEY=你的豆包API密钥
EOF

# 10. 测试启动
gunicorn --bind 0.0.0.0:8888 server:app
# 如果成功启动，Ctrl+C 停止
```

### Step 5：配置 Gunicorn + Nginx（生产环境）

#### 5.1 创建 systemd 服务

```bash
sudo cat > /etc/systemd/system/guidepic.service << 'EOF'
[Unit]
Description=GuidePic - 带拍 AI 拍照指南
After=network.target

[Service]
User=guidepic
Group=www-data
WorkingDirectory=/home/guidepic/app/mobile-tester
Environment="PATH=/home/guidepic/app/mobile-tester/venv/bin"
Environment="DOUBAO_API_KEY=你的豆包API密钥"
ExecStart=/home/guidepic/app/mobile-tester/venv/bin/gunicorn \
    --workers 2 \
    --bind 127.0.0.1:8888 \
    --timeout 300 \
    --keep-alive 75 \
    --log-level info \
    --access-logfile /var/log/guidepic/access.log \
    --error-logfile /var/log/guidepic/error.log \
    server:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

# 创建日志目录
sudo mkdir -p /var/log/guidepic
sudo useradd -r -s /bin/false guidepic 2>/dev/null
sudo chown -R guidepic:www-data /home/guidepic/app/mobile-tester
sudo chown -R guidepic:www-data /var/log/guidepic
```

#### 5.2 配置 Nginx 反向代理

```bash
sudo cat > /etc/nginx/sites-available/guidepic << 'EOF'
server {
    listen 80;
    server_name guidepic.com www.guidepic.com;

    client_max_body_size 25M;  # 照片上传最大 25MB

    # 静态文件直接服务
    location /static {
        alias /home/guidepic/app/mobile-tester/static;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 代理到 Gunicorn
    location / {
        proxy_pass http://127.0.0.1:8888;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # SSE 长连接支持
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
        chunked_transfer_encoding on;
    }
}
EOF

# 启用配置
sudo ln -s /etc/nginx/sites-available/guidepic /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

#### 5.3 启动服务

```bash
sudo systemctl daemon-reload
sudo systemctl enable guidepic
sudo systemctl start guidepic
sudo systemctl status guidepic  # 检查状态
```

### Step 6：SSL 证书（HTTPS）

```bash
# 安装 certbot
sudo apt install -y certbot python3-certbot-nginx

# 自动获取并配置证书
sudo certbot --nginx -d guidepic.com -d www.guidepic.com

# 证书会自动续期（certbot 自带定时任务）
```

### Step 7：验证部署

1. 浏览器打开 `https://guidepic.com`
2. 看到上传页面 → ✅ 部署成功
3. 手机打开 `https://guidepic.com` → 拍照上传测试完整流程
4. 检查 `/health` 端点：`https://guidepic.com/health`

---

## 四、部署架构图

```
Internet
    │
    ├─ guidepic.com (DNS A记录 → 香港服务器IP)
    │
    ▼
[ Nginx :80/443 ]
    ├─ SSL 证书 (Let's Encrypt)
    ├─ 静态文件 (/static/) ─ 直接响应
    ├─ SSE 长连接 ─ proxy_buffering off
    │
    ▼
[ Gunicorn :8888 (内部) ]
    ├─ 2 workers
    ├─ timeout: 300s (AI分析需要时间)
    │
    ▼
[ Flask server:app ]
    ├─ EXIF 提取 → Pillow
    ├─ 视觉分析 → 豆包 API (ark.cn-beijing.volces.com)
    ├─ 方向/方案 → 豆包 API
    ├─ 天气数据 → Open-Meteo API（免费）
    ├─ 地名查询 → OSM Nominatim（免费）
    ├─ Session 管理 → 内存（30分钟TTL）
    └─ 风格缓存 → style_cache.json（本地文件）
```

---

## 五、关键注意事项

### ⚠️ SSE 长连接

你的应用大量使用 SSE（Server-Sent Events）流式传输——这在生产部署时需要注意：

| 组件 | 配置 | 原因 |
|------|------|------|
| Nginx | `proxy_buffering off` | SSE 需要实时推送，不能缓冲 |
| Nginx | `proxy_read_timeout 3600s` | AI分析可能长达几分钟 |
| Gunicorn | `--timeout 300` | worker 超时 5 分钟 |
| Gunicorn | `--keep-alive 75` | 保持连接活跃 |

### ⚠️ 并发控制

当前代码有全局锁 `_processing`（一次只能处理一个请求）。测试阶段够用，但如果多人同时用会排队。

**短期方案**：Gunicorn 2 workers 可以在两个请求排队时分别处理
**长期方案**：改为用户级锁（每个 session 独立）

### ⚠️ 临时文件

当前代码在 `/tmp/` 写入临时照片文件。确保服务器 `/tmp` 有足够空间（至少 500MB）。

### ⚠️ 豆包 API 费用

| 每次使用 | Token 消耗 | 成本 |
|------|------|------|
| 视觉分析 | ~2000 tokens | ¥0.02-0.05 |
| 方向生成 | ~4000 tokens | ¥0.05-0.10 |
| 方案生成（按需） | ~4000-8000 tokens | ¥0.05-0.15 |
| **单次完整使用** | **~10000-14000 tokens** | **~¥0.12-0.30** |

**建议**：在火山方舟控制台设置每日费用告警（如 ¥10/天）。

---

## 六、待办清单

### 🔴 本周内（P0 —— 让朋友能用）

- [ ] 在阿里云万网/腾讯云注册 `guidepic.com`（域名注册）
- [ ] 购买香港轻量应用服务器（2核2G，Ubuntu 22.04）
- [ ] DNS 解析：guidepic.com → 服务器 IP
- [ ] SSH 登录服务器，执行环境搭建脚本
- [ ] 配置 Gunicorn + Nginx + SSL
- [ ] 手机访问 https://guidepic.com 测试完整流程
- [ ] 把链接发给 3-5 个朋友测试

### 🟡 一个月内（P1 —— 完善体验）

- [ ] 注册 `daipai.app` 域名
- [ ] 提交「带拍」第9类商标申请
- [ ] 注册微信公众号「带拍」
- [ ] 注册小红书账号「带拍」
- [ ] 错误页面优化（404/500 友好提示）
- [ ] 移动端 PWA 支持（添加到主屏幕）

### 🟢 远期（P2 —— 正式上线）

- [ ] 购买大陆服务器
- [ ] 提交 ICP 备案
- [ ] 备案完成后 DNS 切换大陆服务器
- [ ] 用户反馈收集系统
- [ ] 使用统计（Google Analytics / 百度统计）
- [ ] GitHub Actions 自动部署
- [ ] Docker 容器化

---

## 七、费用预估

### 阶段一（测试期）

| 项目 | 费用 | 周期 |
|------|------|------|
| guidepic.com 域名 | ¥60-80 | 年付 |
| 香港轻量服务器 | ¥50-70 | 月付（先买1个月） |
| 豆包 API | ¥10-50 | 按量（假设 100 次测试） |
| SSL 证书 | ¥0 | Let's Encrypt 免费 |
| **合计** | **~¥120-200 启动 + ¥110-150/月** | |

### 阶段二（正式上线）

| 项目 | 费用 | 周期 |
|------|------|------|
| 大陆云服务器 | ¥50-150 | 月付 |
| ICP 备案 | ¥0 | 免费（需自己提交） |
| 商标注册 | ¥270+ | 一次性（官费） |
| daipai.app 域名 | ¥80-120 | 年付 |
| **合计** | **~¥470+ 一次性 + ¥130-270/月** | |

---

> 下一步：先注册 guidepic.com 域名，然后买服务器。我可以帮你准备服务器端的完整部署脚本，你只需要复制粘贴执行就好。
